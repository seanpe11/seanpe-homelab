apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helmrelease.yaml
  - kong-config.yaml
  - studio-mcp-nodeport.yaml   # tailnet-only reach for the MCP endpoint
  - secrets.sops.yaml   # generate once with scripts/gen-secrets.py
